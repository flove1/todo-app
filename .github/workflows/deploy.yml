# .github/workflows/deploy.yml
name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Start services with docker-compose
        run: docker-compose up -d --build

      # Optionally, run tests inside the container (adjust service name as needed)
      # - name: Run tests in container
      #   run: docker-compose exec <service-name> npm run test

      # - name: Stop services
      #   run: docker-compose down

      # Uncomment and configure the following step for deployment (e.g., push to registry, remote deploy, etc.)
      # - name: Deploy
      #   run: echo "Add your deployment script here"
